import React from 'react'
import Nav from "../../components/Nav/index";

const Admin = (props) => {
    return (
        <>
            <Nav user={props.user} />
        </>

    )
}

export default Admin